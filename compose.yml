services:
  app:
    build:
      context: .
      dockerfile: app/Dockerfile
    container_name: sbc_api
    ports:
      - "8080:8000"
    volumes:
      - ./app:/app
    environment:
      - FLASK_ENV=development